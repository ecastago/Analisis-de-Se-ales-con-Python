import numpy as np
import matplotlib.pyplot as plt
from scipy import signal, integrate

T = 2
w0 = 2*np.pi/T

def sumatoria_zeta(x):
    sum = 0
    for i in range(x):
        sum += 1/

x = np.linspace(-0.001,10,4000)
xt = signal.square(x)


cm = (5/2)*(np.sinc(m*np.pi*0.5))
# y = np.sin(16*x)


# z = np.e**(-0.75*x)*np.sin(16*x)




plt.plot(x,y)
plt.show()